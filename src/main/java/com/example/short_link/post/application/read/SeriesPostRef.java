package com.example.short_link.post.application.read;

/** A minimal reference to a series member post — just enough to list + link it in a series card. */
public record SeriesPostRef(String slug, String title) {}
