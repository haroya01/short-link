spring:
  datasource:
    url: jdbc:mysql://localhost:3306/short_link?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Seoul
    username: short_link
    password: haroya0117!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: false
  flyway:
    enabled: true
    baseline-on-migrate: true
  data:
    redis:
      host: localhost
      port: 6379
      password: haroya0117!

short-link:
  safe-browsing:
    enabled: false
  pow:
    enforce: false

# query-audit 비활성화 (Spring Boot 4.0.6 multi-context 회귀, 0.3.2 fix 대기).
# 의존성 자체가 build.gradle 에서 빠진 상태라 이 블록은 인식되지 않지만
# 재도입 시 복원용으로 자리만 유지.
# query-audit:
#   enabled: true
#   fail-on-detection: false
#   ...
