# QA 실행 기록 — 2026-07-16

> 대상 리비전: 백엔드 docs/qa-checklist-and-evidence(main+docs, 78bb6ebc) · 웹 main 08cbd87(#882) · kurl-ios main 1ea05ad · kurl-links-ios main 8305ab8(#18).
> 방식: launch-qa-checklist.md 계층 실행. 자동 게이트 전수 + 코드 검증 가능 항목 + 프로드 read-only 스모크.
> 실기기·실계정·부하(k6)·스토어 항목은 수동 표시만 (직영 원칙).

## 결론 한 줄

자동 게이트·프로드 스모크 전부 그린. 신규 회귀 0. 단 **코드 확정 갭 2건**(재단축 거부 부재 · 삭제 잔존행)과 **테스트 인프라 구멍 3건**(링크앱 유닛 타깃 깨짐 · 양 앱 XCUITest CI 부재 · 블로그앱 스냅샷 레퍼런스 재굽기)이 남았다.

## 0. 자동 게이트

| 게이트 | 결과 | 근거 |
|---|---|---|
| 백엔드 `./gradlew build` | PASS | 4m04s. 450 스위트 / 2869 테스트 / 실패 0 / 스킵 7. Redis는 `SPRING_DATA_REDIS_PORT=6380` env 오버라이드 필요(테스트 설정은 6379 하드코딩 — 우회 배선 없음) |
| ArchUnit | PASS | ArchUnitGraphRulesTest 19 + ArchitectureRulesTest 8 + RepositoryUnusedMethodTest 2, 전부 통과. 순환 금지는 slices freeOfCycles 룰. "common→feature 금지" 전용 named 룰은 없음(순환 금지로 간접 강제) |
| Flyway | PASS | 마이그레이션 112개(V1~V114, V54/V107 결번 — 중복 아님). 순번 충돌 0. ddl-auto=validate 하에 전 테스트 컨텍스트 부팅 성공 |
| 쿼리 예산 | PASS | 실명은 `@QueryAudit`(query-audit-starter 0.4.0). runnable 9클래스 전부 통과. link.domain.LinkRepositoryTest 는 @Test 0개 빈 클래스 — 정리 후보 |
| 웹 typecheck/lint/vitest | PASS | tsc 0에러 · lint 경고 1(기존 no-img-element) · vitest 51파일 646테스트 전부 통과, i18n CJK 가드 포함 |
| 웹 e2e 3레인 | PASS(CI) | 최신 main(#882): mock-off·mock-on 둘 다 success — "#771부터 10 fail" 이력 회복 확인. e2e.yml(백엔드 레인)은 SUB_TOKEN 부재로 skip=green — 실행된 게 아님 |
| 웹 `npm run build` | PASS | dev 미기동 확인 후 실행. Compiled successfully, 정적 329/329 |
| iOS 블로그앱 빌드 | PASS | 38s, 위젯 포함. iPhone 17 Pro / iOS 27.0 |
| iOS 블로그앱 kurlTests | 155/160 | 실패 5건 전부 SnapshotSamplesTests — 레퍼런스(kurl-air-27에서 구움)와 런타임 점릴리스 차이로 인한 서브픽셀 노이즈(차이 픽셀 0.06~0.16%, 엣지 AA 국한). 회귀 아님 판정. **kurl-air-27 시뮬에서 SNAPSHOT_RECORD=1 재굽기 필요** |
| iOS 블로그앱 SwiftLint | PASS | error 0 / warning 3(기존, 매직 반경) |
| iOS 링크앱 빌드 | PASS | 25s, 위젯+공유 익스텐션 포함. iPhone 16 Pro / iOS 26.0 |
| iOS 링크앱 유닛 | **실행 불가** | pbxproj 가 디스크에 없는 `SnapshotSamplesTests.swift` 를 참조(스캐폴드 3659e7d 부터) → 테스트 타깃 빌드 실패. LinkDecodingTests 3케이스 검증 불가. **pbxproj 죽은 참조 제거 필요** |
| iOS 링크앱 SwiftLint | PASS | error 0 / warning 26(전부 매직 반경) |
| XCUITest 레인 | **CI 부재** | 양 앱 모두 CI 워크플로는 SwiftLint 단일. UITest 파일은 존재(블로그앱 다수·링크앱 5파일)하나 CI 회귀 감지 없음 — 로컬/수동 전용 |

## 코드 검증 항목 (섹션 1~7 중 자동 확인분)

**PASS**
- 스킴 검증: CreateLinkRequest `^https?://` + @URL + 2048 캡 (§2)
- 종료 상태 HTML 게이트: prod 404 가 text/html + 상태코드 보존 + 마크/비번 폼 렌더 (§2)
- 웹훅 시크릿 암호화: AES/GCM SecretCipher, 저장 시 encrypt, V109 컬럼 확장. 단 TWOFA_AES_KEY 미설정 시 평문 폴백(경고 로그) — 운영 env 전제 (§2·§7)
- 이탈 보호: 에디터 beforeunload + 소프트 내비 dirty 확인 (§3)
- 예약발행 잡 존재: PublishScheduledPostsJob (§3)
- 낙관 토글: 실패 시 롤백 + 에러 토스트(use-optimistic-toggle.ts:158) — 조용한 롤백 아님 (§4)
- 테마: blog=`theme` / kurl=`kurl_theme` 분리, 플랫폼에선 쿠키 단독 진실원(localStorage 폴백 차단, #749 회귀 없음) (§5)
- 날짜 포맷 Asia/Seoul 고정: 전 표면 적용 (§5)
- catch 없는 `.finally()` 스윕: 34곳 전수 0건 (§5)
- 정적 will-change:transform: 페이지 래퍼 0건 (§5)
- 오버레이 포털: QA 윈도우 신규 회귀 0 (§5)
- 정적화: 프리렌더 329(기준선 183 대비 확대 — /use 랜딩 등 자연 증가). 의외의 동적 강등 0 (§6)
- remotePatterns 3호스트 유지 (§6)
- 약관 16/방침 13 섹션 × 5로케일 전부 동기 (§7)
- GPC: 방침 문구("재방문 식별 해시 미생성")와 백엔드 Sec-GPC 처리 3곳 일치 (§7)
- 본문/헤더 캡: 존재 확인. 수치 정정 — 헤더 8KB(체크리스트의 16KB 는 본문 쪽: Tomcat form/swallow 16KB + BodySizeFilter 기본 16KB·라우트별 확대) (§7)

**FAIL — 코드 확정 갭**
1. **자기 자신(kurl.me) 재단축 거부 미구현** (§2): 백엔드 CreateLinkValidator·LinkErrorCode 에 전용 로직/코드 없음, 프론트에도 없음. 리다이렉트 루프/체인 악용 여지. → **PR #631 로 수정**(SELF_REFERENCING_URL 400, apex·www 완전일치만 거부, 갱신 경로 포함, 단위 6+통합 4+갱신·CSV 테스트).
2. ~~계정 삭제 잔존행 미해결~~ → **오탐 정정 (07-16)**: NotificationUserDataEraser(#594·#621)가 common.user.UserDataEraser 중립 포트 경유로 link_notification·notification_preference·blog_notification_preference 3테이블+notification(수신·행위 양방향)을 네이티브 쿼리로 정리, hardDelete 가 userDataErasers.forEach 로 태움. 회귀 테스트(UserDeletionServiceTest.hardDeleteClearsBlogAndSocialRowsBlockingTheUserDelete) 통과 확인. 최초 판정은 네이티브 쿼리 DELETE 가 repo 메서드 grep 에 안 걸려 생긴 오독.

**주의 — 체크리스트 기준과 불일치(실해 없음)**
- 관리자 경로(§7): /admin 이 하드 404 가 아니라 200 앱셸(클라이언트 가드 + noindex,nofollow). 로그인 바운스는 아니고 서버 렌더 데이터 누출도 없음. 존재 은닉 기준을 유지하려면 서버측 404 필요 — 판단 대기.
- 존재하지 않는 글(§9): UI 는 정상 not-found 렌더지만 HTTP 200 (soft-404). sitemap 이 실글만 실어 크롤러 영향 미미.

## 9. 프로드 스모크 (read-only, 07-16 16:00 KST 대)

9/9 PASS. kurl.me 307→/ko 200 · blog.kurl.me 200 · sitemap 219 URL 중 글 표본 5개 전부 200(전량 404 사고 재발 없음) · RSS /feed 의 링크 호스트 전부 blog.kurl.me, vercel.app 누출 0 · 무작위 숏코드 404 HTML 게이트 · robots 2종 200 · prod-smoke CI 최신 success(7연속, 07-15 20:13 UTC 1회 실패는 다음 런에서 자가 회복) · not-found 5로케일 렌더.
미실행: 활성 코드 클릭 +1(실링크 카운트 조작 금지) · 실계정 발행 완주(수동).

## 수동/직영 잔여 (자동화 불가 — 표시만)

- §1 전체: 실계정 Google/Apple 로그인, 계정 전환 잔존, 게스트 claimToken 귀속 실측, zip 내보내기
- §2: maxViews 동시 경계(k6) · QR 스캔 실측 · SSE 부하 leak
- §3~4: 표/이미지/노션 붙여넣기·시리즈·리비전·하이라이트 등 실조작 (e2e 레인이 write/reader 핵심 경로는 커버)
- §5: AXL 캡처·VoiceOver·기내모드 (직전 사냥 라운드 #882/#188 증거 스크린샷 49장 루트에 있음 — 커밋 여부 결정 대기)
- §6: Lighthouse 재측정 · DB 풀 부하
- §8 전체: 실기기 devicectl·위젯·푸시·TestFlight·스토어 자산

## 후속 액션 상태 (07-16 수정 라운드)

1. ~~hardDelete notification 정리~~ — 오탐, 기수정 확인 (위 정정 참조)
2. kurl.me 재단축 거부 — **PR #631** (백엔드, 갱신 경로 포함)
3. kurl-links-ios 유닛 타깃 수리 — **PR #19** (pbxproj 실파일 배선 + 픽스처 드리프트 수리 + unit-tests CI 레인. 단 repo Actions 는 사용자 결정으로 계속 OFF — 워크플로는 파일로만 병합, 재활성화 시 자동 동작)
4. kurl-ios 스냅샷 레드 — **PR #189**. 진단 정정: 러너 불일치가 아니라 완전일치(precision 1.0) 비교가 런타임 점릴리스 AA 흔들림에 취약한 것(기준 시뮬에서도 red 재현). 레퍼런스 PNG 보존, precision 0.99/perceptual 0.98 완화. 기준·비기준 시뮬 5/5 통과 + 다크 바꿔치기로 진짜 회귀 검출 유지 확인. 참고: 이 스냅샷은 CI 미실행(로컬 전용 가드), SourcePackages 2경로 gitignore 누락 위험
5. XCUITest CI — 호스티드 러너 Xcode 26.6 까지라 iOS 27 앱(블로그) 불가. 블로그앱은 ci/ios-uitests 브랜치에 informational 워크플로 기존재(미머지). 링크앱은 PR #19 에 유닛 레인 포함(Actions OFF 상태)
6. /admin 서버측 404 · 글 soft-404 — **PR #883**. soft-404 는 generateMetadata 단계 notFound()(Next 14 스트리밍 레이아웃이 페이지 notFound() 상태코드를 못 얹는 한계 우회)로 글·작가·시리즈 라우트 실측 404. /admin 은 익명에 서버 하드 404(7라우트 서버 page 가드) — 관리자 판별은 서버 불가(로그인 신호 kurl_has_session 쿠키까지), 로그인 사용자는 기존 클라 가드 유지. 한계: blog/admin 은 상위 클라 레이아웃이라 상태코드 200(셸 은닉+클라 404 유지). 게이트 4종+e2e admin-404 7건+신규 유닛 4건 통과
